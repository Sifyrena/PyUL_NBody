# PyUL 3.0.0 "First Light" - BH Absorption MVP + N-Body/FDM Coupling Fixes

(Entries below span the run-up from 2.40.3 through this first tagged release.)

- 24 Apr 2026: FEATURE: Removed runtime momentum and angular momentum evaluations. The quantities can be reconstructed if you save the wavefunction.
- 24 Apr 2026: BUGFIX: QuickInterpolate(), which is needed for particle dynamics, is put back to the Particles pipeline with cosmetic changes. Thanks Claude.
- 24 Apr 2026: COSMETIC: Removed IPython Reference: clear_output()
- 24 Apr 2026: BUGFIX: all references of `a` fully renamed to `smoothing` for Plummer Sphere Softening.
- 24 Apr 2026: BUGFIX: Flag `Use_EP` defaults to False.
- 24 Apr 2026: COSMETIC: Save Flags are now much more readable. New method save_flag_index("2Density") replaces a hardcoded index in Evolve().
- 24 Apr 2026: COSMETIC: Removed a 0+0 Calculation in Soliton Init.
- 24 Apr 2026: COSMETIC: Moved Green Functions to Integrate subfolder.
- 22 Sep 2026: BUGFIX: `ExtPhi` default (`0`, an int) crashed `resolve_ext_phi()` on every run that didn't explicitly set an external potential. Now skipped entirely when `ExtPhi == 0`.
- 22 Sep 2026: FEATURE: MVP for BH absorption. New `config.BlackHole["Sink"]` block (`Flag`, `ParticleIdx`, `Amplitude`, `Radius`, `Feedback`) sources a Gaussian imaginary potential centred on a chosen matter particle's *current* position each step, damping `psi` locally (`psi *= exp(-h*V_sink)`); absorbed mass is added onto that particle's entry in `masslist` when `Feedback` is on. Logged per-save to `Outputs/BHMass.npy`. Mass loss from the grid and mass gain on the particle were checked to match in a smoke test.
- 22 Sep 2026: COSMETIC: Moved the BH sink logic out of the main loop and into `Particles/Accretion.py` as `ApplySink()` (was a 0-byte stub). `Evolve.py` now just calls it. Re-verified with the same smoke test — byte-identical output to the inline version.
- 22 Sep 2026: FEATURE: Added `BHAccretionDemo.uldm`, an example config exercising `BlackHole.Sink` end to end (32³ grid, one soliton, one gravitating matter particle, `Amplitude=50, Radius=0.05`). Run: `Evolve('BHAccretionDemo.uldm')`. The particle is gravitationally bound to the soliton and oscillates back through the core, so `Outputs/BHMass.npy` climbs in repeated bursts each time it re-enters the dense center; `Outputs/ULDMass.npy` drops by the exact matching amount at every save (verified to ~1e-13 relative).
- 22 Sep 2026: BUGFIX (LEGACY cleanup): Removed the `LEGACY.Shift` (`GridShift`) and `ADVANCED.ComputeQuad` (`QuadrupoleSecond`) call sites from `Evolve.py` — both referenced functions that were never defined anywhere in the codebase (leftover PhD-era references per author), so either flag would NameError if ever flipped on. Confirmed via re-run that this is pure dead-code removal: `BHAccretionDemo.uldm` output is byte-identical before/after.
- 22 Sep 2026: BUGFIX: `Modifier.UniformFieldAddOn` (the uniform-background ULDM feature) was completely broken — `density_value`/`uniform_velocity` were read from config but never actually assigned to the `Density`/`UVel` variables used to build the field, so enabling `Flag=True` guaranteed a `NameError` at init. Fixed in `Evolve.py` by assigning `Density = density_value` and `UVel = uniform_velocity` when `Uniform` is set (previously only the `else` branch, zeroing both, was ever populated).
- 22 Sep 2026: FEATURE: Added `BondiHoyleCalibration(M, v, cs=0.0)` to `Particles/Accretion.py` — derives `Sink.Amplitude`/`Sink.Radius` that make `ApplySink`'s early-time absorption rate reproduce the classical Bondi-Hoyle-Lyttleton rate `Mdot = 4*pi*(GM)^2*rho/(v^2+cs^2)^1.5` (G=1, code units): `Radius = 2M/(v^2+cs^2)` (Bondi radius), `Amplitude = (v^2+cs^2)^1.5/(8*sqrt(2*pi)*M)`. Validated with a 7-point scan (uniform ULDM background, particle moving through it at fixed velocity, 64³ grid) spanning v, M, and rho0 independently — measured early-time `Mdot` matched the analytic BHL prediction to within 0.4-1.0% at every point. Scan script and results kept in scratch, not committed (see `BondiHoyleCalibration_scan.png` for the comparison plot).
- 22 Sep 2026: FEATURE: `Sink.Dynamic` (config.BlackHole["Sink"]) — when `True`, `ApplySink` ignores the fixed `Amplitude`/`Radius` and recomputes them every step from the sink particle's *current* mass and speed via `BondiHoyleCalibration()`, so a particle on a bound/oscillating orbit (like `BHAccretionDemo.uldm`) gets a genuinely time-varying, locally-correct accretion rate instead of one fixed snapshot value. Speed is floored at `Sink.VFloor` (default 0.05) to keep the Bondi radius finite at orbital turning points, and `Radius` is separately capped at `Sink.RadiusCap` (default `0.25*BoxLength`) since `R ~ M/v^2` can otherwise run past the box as the sink grows and slows — capping `R` alone (not `Amplitude`) under-delivers relative to the naive divergent rate, which is the standard low-velocity regularization for sink particles. `BHAccretionDemo.uldm` now uses `Dynamic=True`.
- 22 Sep 2026: BUGFIX: `BHAccretionDemo.uldm` was using `BoxLength=1` with a mass-10 soliton whose fitted core radius is ~0.29 code units — density at the box edge was still **11.4% of peak**, meaning the soliton's periodic images (this is a periodic FFT grid) were overlapping and contaminating both the gravity solve and ψ's free-particle evolution. This affected every run/plot of this demo so far (including ones already shown). Fixed by increasing `Space.Box.BoxLength` to 3 and `Space.Resolution` to 64 (keeps the same grid spacing as before, `dx=1/32`... now `3/64≈1/21.3`, still resolved) — edge density fraction is now `9.3e-6`. With the contamination gone, the demo's actual dynamics changed materially: instead of wide, erratic excursions and an apparent soliton "splitting" (which was the periodic-image artifact, not real physics), the particle now sinks smoothly toward the core and settles near the center by the end of the run, with the sink mass growth curve correspondingly smoother (rises then plateaus around t≈0.8, final mass 1.55 vs. the earlier contaminated run's 0.92). Mass conservation unaffected either way (still exact to ~1e-12 relative) since that check doesn't depend on box size.
- 22 Sep 2026: Bumped `BHAccretionDemo.uldm` to `Resolution=192` (user requested higher fidelity, disk cost is negligible since only 2D/1D slices + particle/energy logs are saved — 12MB total, no full 3D fields). Took ~44 min on the `Co` env (run in background). Result is consistent with the `Resolution=64` run (final BH mass 1.529 vs 1.550, ~1.4% difference) — confirms 64 was already essentially converged, this just gives visually cleaner density contours. Plots updated in place.
- 23 Sep 2026: FEATURE: `Sink.ConserveMomentum` (config.BlackHole["Sink"]) — the sink was previously a pure mass leak: it grew the particle's mass with zero recoil, unphysical since real accretion carries a drag force set by the relative velocity of the swallowed gas. Fixed by depositing the absorbed ULDM's momentum onto the particle. Derivation: `psi *= exp(-h*V_sink)` is a real multiplicative factor, so it rescales amplitude but leaves phase - and therefore the local velocity field `v(x)=j(x)/rho(x)` with current density `j=Im(psi* grad psi)` - unchanged. The momentum removed is `integral of j(x)*(1-exp(-2*h*V_sink(x))) dV`, computed via a spectral (FFT) gradient of psi, and added to the particle's momentum (mass-weighted, then divided by the new mass - a rocket-equation update since mass and velocity change together). Costs 3 extra FFTs per step (`np.fft.fftn`/`ifftn`, not the existing pyfftw plans - kept separate for simplicity/correctness). `BHAccretionDemo.uldm` now sets `ConserveMomentum=true`.
- 23 Sep 2026: BUGFIX: found while testing the above with `Saving.Flags` that omitted `"Energy"` - `egpcmMlist.append(EGPCM)` in the main loop was unconditional, but `egpcmMlist` is only initialized when the `Energy` save flag is on, so any run without it crashed with `UnboundLocalError`. Pre-existing bug, unrelated to the momentum feature; fixed by gating the `.append()` the same way the initialization is gated.
- 23 Sep 2026: VALIDATION: built a direct check of total (field + particle) momentum conservation - ran the same soliton-eating setup three ways (48³, short duration) and computed `integral(Im(psi* grad psi)) + M*v` at every save from saved wavefunction snapshots. Gravity-only baseline (no sink at all) has residual numerical drift ~1e-3 (grid/interpolation noise, not a bug). Sink **without** `ConserveMomentum`: drift -0.35 (~310x the gravity floor - confirms the leak was real and large). Sink **with** `ConserveMomentum`: drift +0.0055 (~5x the gravity floor) - a 64x improvement, landing close to the pre-existing numerical noise floor rather than at machine precision. The residual gap is worth understanding better (candidates: finite-h discretization in the momentum integral, or it's simply consistent with the same grid/interpolation noise the baseline shows) but this is strong confirmation the fix works as intended. Re-ran `BHAccretionDemo.uldm` at resolution 64 with `ConserveMomentum=true`: mass conservation still exact (1.6e-12), but the growth curve changed materially (final mass 0.735 vs. 1.55 without momentum feedback) since the particle now genuinely feels drag/kicks from what it swallows instead of gaining mass for free. Plots updated in place.
- 24 Sep 2026: BUGFIX (inherited from "The Original PyUL NBody"): `FWNBody`/`FWNBody_NI` in `Integration/NBodyAdvance.py` silently zeroed the particle's gravitational force from the ULDM field whenever it came within ~2 grid cells of any box face, instead of erroring or wrapping - a numerical safety net mistaken for physics. For a periodic domain (`BC.IsoP=False`, the common case) this is wrong: the field genuinely continues across the boundary. Fixed by wrapping the local 4x4x4 force-interpolation patch periodically (`np.arange(...) % resol`) whenever `IsoP=False`; the old hard-zero fallback is kept for `IsoP=True` (isolated/vacuum potential), where there's no periodicity to wrap into. `NBodyAdvance`/`NBodyAdvance_NI` gained a `Periodic` parameter (`Evolve.py` passes `Periodic = not IsoP`), defaulting to `True` to match the periodic case that's actually common. Applies to every run with matter particles, sink or not. Verified with a synthetic sinusoidal-potential unit test (force near the edge now tracks the analytic gradient instead of reading zero) and confirmed byte-identical output for particles away from the boundary (the existing smoke test's particle never approaches the edge).
- 24 Sep 2026: BUGFIX: the BH sink (`ApplySink`) was being applied to `psi`/`rho` at the particle's position from the *end of the previous step*, while the Newtonian back-reaction `phiTM` the field feels is (correctly) built at the particle's freshly-advanced position later in the same step - a one-substep temporal mismatch between the sink's imaginary-potential channel and the real/gravitational channel. Fixed by moving the `ApplySink` call in `Evolve.py`'s main loop from just after `rho` is recomputed to just after `NBodyAdvance`/`NBodyAdvance_NI`, so absorption and `phiTM` are now evaluated at the same position. Side effect: the Poisson solve that determines the particle's own motion this step now uses the pre-absorption density (mass not yet swallowed still gravitates on the particle as it approaches - a defensible physical reading: absorption and its Newtonian deposit happen together at the step's end, co-located with the particle's arrival position) rather than the post-absorption density used previously. No effect when `Sink.Flag=False` (the block is a no-op) - confirmed via the existing smoke test, which reproduced the exact non-sink mass-conservation baseline and the sink case's mass-conservation to ~1e-8 relative of the previous run's numbers (the tiny difference is the intended physics change, not noise).